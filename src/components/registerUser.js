import React, {Component} from 'react';
import Nav from './nav';

class RegisterUser extends Component {
    render(){
        return(
            <div>
                Register User
            </div>
        )
    }
}

export default RegisterUser;